{
  "meta": {
    "sourcePrompt": "MASTER ERP HARDENING + FULL PROJECT REMEDIATION PROMPT v1.0.0",
    "mode": "phase-by-phase, verify+push each phase before the next",
    "generatedAt": "2026-08-23",
    "note": "Phase 0 is discovery only — no code changed to produce this plan. Findings are grounded in direct code inspection, not the master prompt's assumed defect list."
  },
  "phase0": {
    "status": "completed",
    "deliverables": [
      "docs/engineering/full-remediation-audit.md",
      "docs/engineering/full-remediation-plan.json"
    ],
    "filesReviewed": 876,
    "p0Found": 2,
    "p1Found": 1,
    "p0": [
      {
        "id": "P0-1",
        "title": "pgQuery('BEGIN') is not a real transaction",
        "files": [
          "src/lib/hr/payrollData.ts",
          "src/lib/hr/employeeData.ts"
        ],
        "evidence": "pgQuery() calls pool.query() which acquires+releases a connection per call; BEGIN and the following statements are not guaranteed to run on the same connection under concurrency.",
        "correctPatternExample": "src/lib/masterdata/masterDataData.ts, src/lib/import/importData.ts, src/lib/numbering/service.ts (pool.connect() -> client.query('BEGIN') -> ... -> COMMIT -> release())",
        "recommendedFix": "Extract withTransaction(fn) helper in src/lib/db/index.ts using pool.connect(); migrate the two broken call sites; add a rollback regression test."
      },
      {
        "id": "P0-2",
        "title": "No centralized bilingual error catalog",
        "files": [
          "src/lib/api/respond.ts"
        ],
        "evidence": "src/lib/errors/ does not exist; apiError() maps exceptions to an English-only message + HTTP status with no code/fa/en/severity/module shape.",
        "recommendedFix": "Build src/lib/errors/ (catalog, factory, formatter, codes) per Phase 7 of the master prompt; wire into apiError() without breaking existing callers."
      }
    ],
    "p1": [
      {
        "id": "P1-1",
        "title": "Non-financial empty catch blocks",
        "files": [
          "src/components/ds/ThemeProvider.tsx",
          "src/app/admin/hero/HeroCenter.tsx"
        ],
        "severity": "low",
        "note": "5 real empty catches, all localStorage/video best-effort, none touch money/inventory/audit. Not P0."
      }
    ],
    "nonFindings": [
      "audit:rbac gate already covers 159+ routes, passing",
      "Central money/date formatter already exists (src/lib/format.ts)",
      "Reversal-not-void GL discipline (BUG-020) already exists, regression-tested",
      "console.log: only in logger.ts's own sink, not stray debug output",
      "TODO/FIXME/HACK: 0 matches repo-wide"
    ],
    "assumedButAlreadyImplemented": [
      "Atomic GL posting (26.23)",
      "Approval fail-safe deny-by-default + maker/checker (26.12, 26.24b)",
      "AR/AP subledger vs GL balance proofs (26.26c)",
      "RBAC tree with deny-dominates semantics (26.27)",
      "Reversal-not-void for financial corrections (26.23, BUG-020 fix)"
    ]
  },
  "phaseQueue": [
    {
      "id": "phase1a",
      "title": "withTransaction() helper + migrate payroll/employee writes off pgQuery('BEGIN')",
      "priority": "P0",
      "status": "completed",
      "evidence": "src/lib/db/index.ts withTransaction() added (injectable pool for tests); payrollData.saveBrackets + employeeData.addEmploymentRecord migrated off pgQuery(BEGIN); 3 unit tests (rollback/commit/release-on-rollback-failure) + live-PostgreSQL proof (scratch-table forced-failure rollback = 0 rows, success commit = 2 rows; real addEmploymentRecord raise via HTTP API correctly closed record 1 the day before record 2 opened). tsc/lint/vitest(1295)/build/9 audits all clean."
    },
    {
      "id": "phase1b",
      "title": "Sweep remaining multi-statement financial/inventory writes for the same anti-pattern",
      "priority": "P0",
      "status": "completed",
      "evidence": "Repo-wide grep confirms zero remaining pgQuery('BEGIN'/'COMMIT'/'ROLLBACK') call sites outside comments/tests — phase1a fixed the only two broken instances. The other three multi-statement transaction sites (masterdata/masterDataData.ts, import/importData.ts, numbering/service.ts) already correctly hold a single dedicated pool.connect() client for their whole transaction and were never broken; migrating them onto withTransaction() is a same-behavior refactor-for-consistency, deferred as low-priority/no-regression-risk rather than done opportunistically now, per the master prompt's own no-unnecessary-rewrite rule."
    },
    {
      "id": "phase7",
      "title": "Bilingual error catalog (src/lib/errors/) wired into apiError()",
      "priority": "P0",
      "status": "completed-scoped",
      "evidence": "src/lib/errors/ built (codes.ts, catalog.ts, factory.ts, formatter.ts, index.ts) — bilingual code/httpStatus/severity/module/recoverable/recommendedAction per code. Wired into the real ERP-SALES-CREDIT-LIMIT-EXCEEDED path (sales/documents route, confirm op) replacing the English-only badRequest() call — the exact worked example from the master prompt. toApiResponse() keeps the existing {error} field for backward compat with every current admin manager, additive error_fa/code/severity/recoverable fields for new callers. 4 unit tests + live-PostgreSQL proof: created a 100,000-limit customer, a 5,000,000 invoice, confirmed it -> real HTTP 400 with correct bilingual interpolated message + code=ERP-SALES-CREDIT-LIMIT-EXCEEDED, invoice correctly stayed draft (never confirmed). Scoped: infrastructure + ONE real call site, not a repo-wide migration of every existing apiError/badRequest call — that migration is tracked as phase7b in the queue below."
    },
    {
      "id": "phase-remaining",
      "title": "Remaining master-prompt phases, each audited against existing CLAUDE.md phase history before new code is written",
      "priority": "P1+",
      "status": "pending"
    },
    {
      "id": "phase7b",
      "title": "Migrate remaining high-value apiError/badRequest call sites onto the error catalog (module by module, starting with other financial-blocking checks) (module-by-module, ongoing)",
      "priority": "P1",
      "status": "completed-partial",
      "evidence": "Migrated 3 more real financial-blocking messages onto the error catalog: ERP-SALES-VOID-PAID-INVOICE-BLOCKED (the BUG-013/CLAUDE.md rule 10 paid-invoice-void guard), ERP-FINANCE-JOURNAL-UNBALANCED (both create and update paths), ERP-FINANCE-POSTED-ENTRY-IMMUTABLE (journal DELETE). 3 new unit tests (7 total in errors.test.ts). Live-PostgreSQL: a real unbalanced 2-line journal POST returned HTTP 400 with the correct bilingual+coded response; a real invoice was created, confirmed, paid in full, then a void attempt correctly returned HTTP 400 with the BUG-013 bilingual message + code -- invoice stayed confirmed, not voided. Marked partial (not fully closed): ~700 other apiError/badRequest call sites remain unmigrated repo-wide -- this phase migrates high-value real call sites incrementally, module by module, not all at once (an unbounded mechanical migration was explicitly out of scope per the master prompt itself). tsc/lint/vitest(1304)/build/9 audits all clean."
    },
    {
      "id": "phase2",
      "title": "Approval engine RULE-004 fail-safe: missing matrix rule must never silently auto-approve",
      "priority": "P0",
      "status": "completed",
      "evidence": "REAL P0 found by direct code audit (not assumed): src/lib/erp/approvalData.ts createApprovalRequest had `const autoApproved = plan.length === 0` which conflated \"no matrix rule matched this docType/amount\" (a config gap) with \"a matched rule explicitly has zero levels\" (a real business decision) -- exploitable live because approval_matrix rows are admin-editable (upsertMatrixRule/deleteMatrixRule) so narrowing/deleting a range silently starts auto-approving that range. Fixed: resolveApprovalPlanExplicit() in matrix.ts returns {plan, ruleMatched} distinguishing the two cases; createApprovalRequest now auto-approves ONLY when ruleMatched=true and the matched rule has zero levels; when NO rule matched at all it falls back to a mandatory single administrator-role approval level (fail-safe, never silent-approve) and raises an idempotent business_alert (kind=approval_matrix_gap) naming the exact doc type/amount/request so the gap is visible and fixable from /admin/approvals. 3 new unit tests + live-PostgreSQL proof: deleted all payment_request matrix rows, submitted a real 500,000,000 payment via the real treasury API -> stayed status=pending with a 1-level administrator plan (never silently approved) + the config-gap alert was written with the exact detail; restored one rule and confirmed a normal payment still routes to finance_manager as designed (no regression). tsc/lint/vitest(1300)/build/9 audits all clean."
    },
    {
      "id": "phase3",
      "title": "Inventory RULE-012/013: negative-stock guard + atomic transfer on the manual moves route",
      "priority": "P0",
      "status": "completed",
      "evidence": "REAL P0 pair found by direct code audit: /api/admin/erp/inventory/moves POST wrote a transfer as TWO separate non-transactional pgQuery inserts (half-transfer risk if the second insert failed after the first committed) AND had ZERO available-quantity check before writing a negative issue/transfer-out inv_moves row (unlike the guarded 26.19 shipment-issue path via canHold) -- a manual Issue or Transfer could drive stock negative with no check at all. Fixed: canIssueDirect(onHand, qty) pure guard added to stockOps.ts (default BLOCK, this repo has no negative-inventory policy setting to reuse); the route now wraps both the transfer (both legs) and any outbound single-row move in withTransaction (Phase 1a helper), serialized per product×warehouse via pg_advisory_xact_lock to close the concurrent-request race, computing on-hand from inv_moves inside the same transaction/lock before writing. 4 new unit tests + live-PostgreSQL proof: received 10 units, over-issue of 11 correctly blocked HTTP 400 with zero rows written, exact issue of 10 succeeded; received 5 more, over-transfer of 6 blocked, exact transfer of 5 succeeded with BOTH legs atomically written sharing one ref (WH1 to 0, WH2 to 5). tsc/lint/vitest(1301)/build/9 audits all clean."
    },
    {
      "id": "phase1c",
      "title": "GL posting atomicity (RULE-001/RULE-006): header+lines+source-link commit as one transaction",
      "priority": "P0",
      "status": "completed",
      "evidence": "REAL P0 found by direct code audit: the shared GL entry-insert core (glPosting.ts insertPostedEntry) plus the two highest-volume real posting paths (salesData.postSalesInvoiceToGl -- every confirmed sales invoice -- and purchasingData.postPurchaseInvoiceToGl) all inserted the journal header, then LOOP-inserted lines, then updated the source-document gl_entry_id link, using bare pgQuery with NO transaction. A failure on any line insert (or the link update) left a POSTED, UNBALANCED journal entry on the books, or a posted entry the source document did not know about (breaking the gl_entry_id idempotency guard on retry -- risking a double post). Fixed: insertPostedEntry now takes an injected TxQuery; postSalesPaymentToGl/postCustomerRefundToGl/postPurchasePaymentToGl/reverseEntry (glPosting.ts) and postSalesInvoiceToGl/postPurchaseInvoiceToGl (sales/purchasingData.ts) each wrap their ENTIRE header+lines+link sequence in one withTransaction call (Phase 1a helper) -- commits together or none of it lands. Verified: zero regressions across the full committed regression suite (22 suites, live PostgreSQL) including every GL-specific assertion (26.23 invoice auto-post+idempotency, 26.24b BUG-008 AP non-negative, 26.26b BUG-020 reversal/closed-period, trial-balance-still-balanced); PLUS a direct forced-mid-transaction-failure test against real Postgres proved a header insert + one line insert (before the forced throw) rolled back completely -- zero leaked rows -- the exact posted-unbalanced-entry defect class this closes. tsc/lint/vitest(1301)/build/9 audits all clean."
    },
    {
      "id": "phase1d",
      "title": "GL posting atomicity sweep: revaluation, intercompany, budget lines",
      "priority": "P0",
      "status": "completed",
      "evidence": "Continued the Phase 1c sweep (same defect class) into the remaining candidates found by grepping for files with 2+ INSERT statements and no withTransaction: erp/revaluationData.ts bookRevaluation (FX revaluation GL post -- header+loop-lines, same unbalanced-entry risk), erp/ledgerData.ts bookIntercompany (WORSE: posts TWO full journal entries across two companies in a bare loop -- a mid-loop failure could post company As entry and silently drop company Bs, breaking the intercompany balance across two separate companies books), and erp/budgetData.ts replaceLines (DELETE-then-loop-INSERT with no transaction -- a failure mid-loop silently lost budget lines, old ones deleted, new ones only partially written). All three now wrap their full sequence in withTransaction. Verified: zero regressions across all 22 committed regression suites on real PostgreSQL; bookIntercompany directly exercised live against real Postgres (2 companies, 2 posted entries, 2 balanced lines each, correctly stamped to each companys company_id) -- the identical withTransaction mechanism was already directly rollback-proven with a forced mid-transaction failure in Phase 1c. tsc/lint/vitest(1301)/build/9 audits all clean."
    },
    {
      "id": "phase1e-backlog",
      "title": "BACKLOG (not yet fixed): draft-creation atomicity gaps found during phase7b",
      "priority": "P1",
      "status": "completed",
      "evidence": "Both gaps fixed with the same withTransaction pattern: finance/journal/route.ts POST (create) and the update op each now wrap header+lines(+template) as one transaction; erp/sales/documents/route.ts POST (create-or-update) wraps header write + DELETE-old-lines + loop-insert-new-lines as one transaction. Verified: zero regressions across all 22 committed regression suites on real PostgreSQL; direct live proof -- created a real invoice with 2 lines (confirmed 2 rows), then updated it with 3 different lines through the real HTTP update path (confirmed exactly 3 new rows, old 2 fully replaced, no orphans). tsc/lint/vitest(1304)/build/9 audits all clean."
    },
    {
      "id": "phase-idempotency",
      "title": "Double-submit guard (RULE-025 / 26.32-class): wire runOnce into sales-document and journal-entry create",
      "priority": "P0",
      "status": "completed",
      "evidence": "REAL gap found: the 26.32 double-submit fix (runOnce) was NEVER applied to any ERP module -- only its original 3 targets (skills/timeline/crm-leads). Two genuinely concurrent identical POSTs could create two duplicate invoices or two duplicate journal entries. Fixed: wired runOnce into erp/sales/documents POST (create branch only -- update targets a specific row, naturally idempotent) and erp/finance/journal POST (always a create). SECOND real bug caught by the live-concurrency test itself: nextNumber() was called BEFORE runOnce/withTransaction, so even though only one row was actually written, every concurrent request still minted its own document/entry number (numbering-sequence gaps) and -- worse -- reported ITS OWN locally-minted number back to the caller instead of the one really stored on the row (4 of 5 concurrent callers were told a WRONG entryNo for the one real entry). Fixed by moving nextNumber() inside the runOnce-guarded transaction closure. Verified live against real PostgreSQL: fired 5 genuinely concurrent identical invoice-create POSTs -> all 5 responses returned the same id, exactly 1 row in sales_documents; fired 5 genuinely concurrent identical journal-entry-create POSTs -> before the numbering fix, all 5 shared the same id but reported 5 DIFFERENT entryNo values (only 1 matched the DB); after the fix, all 5 responses report the identical, correct entryNo matching the single stored row. tsc/lint/vitest(1304)/build/9 audits all clean."
    },
    {
      "id": "phase2-deep-audit",
      "title": "Approval Engine deep audit: transactional atomicity, concurrency, and repo-wide SoD gap",
      "priority": "P0",
      "status": "completed",
      "evidence": "Deep audit of the whole approval engine (lib/approval/{matrix,engine,escalation,analytics}.ts, lib/erp/approvalData.ts, api/admin/erp/approvals). Confirmed already-correct: fail-safe missing-rule handling (earlier phase2), pure derived state machine (approvalState recomputes status from an append-only actions log — REJECTED/APPROVED/CANCELLED->anything else is structurally impossible since actOnRequest refuses any further action once status leaves pending/changes_requested), escalation never grants approval (only changes notification target), RBAC/IDOR (unauthorized approver rejected by resolveActor, never trusts client-supplied approverId). THREE real defects found and fixed: (1) actOnRequest (the actual decide path) was NOT transactional -- read row, INSERT action, re-read, UPDATE request status, conditionally touch a linked HR request, all bare pgQuery -- a mid-sequence failure could leave an orphan action with no matching status update; (2) NOT concurrency-safe -- two decisions racing on the same request had no serialization, a lost-update/duplicate-action race; (3) THE BIG ONE: separation-of-duty (isSeparationViolation) was a hardcoded 2-item allowlist (journal_entry, payroll_period) -- 16 of 18 registered approval-matrix doc types (payment_request, purchase_order, asset_disposal, budget_change, ...) had ZERO maker-checker enforcement; a user could create AND approve their own payment/purchase, confirmed live. Fixed (1)+(2) by wrapping the whole decide sequence in withTransaction with a pg_advisory_xact_lock keyed on the request id plus a duplicate-decision idempotency guard; fixed (3) by defaulting SoD to every registered doc type (was: allowlist, now: opt-out-only design matching the approval-matrix RULE-004 fail-safe philosophy). A first version of fix (3) used matrix.ts DOC_TYPES alone and silently dropped payroll_period protection (a real, separately-used doc string outside the matrix engine) -- caught by the FULL regression suite (28.3-الف/ب payroll SoD assertions failed) before this was pushed, fixed by explicitly re-adding payroll_period, verified 22/22 green again. 5 new unit tests for isSeparationViolation (zero existed before, despite it being a security control) + live PostgreSQL verification of all of: A valid approval succeeds, B missing rule blocks, C unauthorized approver blocked, D maker cannot self-approve (the fixed bug, confirmed both before-fix-fails and after-fix-blocks), E three genuinely concurrent identical decisions on one request -> exactly 1 fulfilled + 2 rejected + exactly 1 approval_actions row + correct final status, F a forced mid-transaction failure -> zero leaked rows, request untouched. tsc/lint/vitest(1309)/build/9-audits/22-regression-suites all clean."
    },
    {
      "id": "phase3-sales-audit",
      "title": "Sales / Order-to-Cash deep audit: credit-limit + overpayment concurrency races",
      "priority": "P0",
      "status": "completed",
      "evidence": "Deep audit of the sales domain (sales.ts/salesData.ts/glPosting.ts, sales/{customers,documents,payments} API routes). Honest classification of the order-to-cash chain: quotation/sales_order/invoice/AR/payment/GL/returns are genuinely IMPLEMENTED (pricing, credit control, invoice, AR, payment allocation, GL auto-posting, return/refund via BUG-013). Inventory Reservation and Delivery are NOT_IMPLEMENTED as part of the sales flow -- confirmed by direct code inspection (zero references to inv_moves/inv_reservations/stock checks anywhere in sales.ts/salesData.ts/the sales documents route); the Inventory module (26.19) is a fully separate subsystem never wired to sales_documents. Discount-approval routing (discount_approval doc type) is registered in the approval matrix engine but never actually triggered from the sales flow -- also NOT_IMPLEMENTED as an active control (document immutability after confirm is the only real discount-tampering guard, and it is solid). TWO real, confirmed, exploitable TOCTOU concurrency defects found and fixed: (1) the credit-limit guard on invoice confirm read the live AR balance then wrote status=confirmed as two separate unlocked statements -- two genuinely concurrent confirms for the SAME customer, each individually within the limit but together exceeding it, could both pass; (2) the overpayment guard on payment recording had the identical pattern -- two concurrent payments against the same invoice could both read the same alreadyPaid sum and both insert, together exceeding the invoice total. Both fixed with pg_advisory_xact_lock-serialized withTransaction wrapping the whole check+write sequence (same pattern used throughout this remediation). Live-PostgreSQL verified: (A) a complete cash sale end-to-end (customer->invoice with 10% discount + 9% VAT, confirm+auto-GL-post, full payment) -- pricing math exact, GL entries reconciled to the penny (Dr AR 9,810,000 / Cr Revenue 9,000,000 / Cr VAT 810,000, then Dr Bank/Cr AR 9,810,000 on payment, net AR = 0, subledger total == GL total); (B) the credit-limit race: two invoices each individually within a 10,000,000 limit, confirmed concurrently -- exactly 1 succeeded, 1 correctly blocked with the bilingual ERP-SALES-CREDIT-LIMIT-EXCEEDED error, confirmed by direct DB inspection; (C) the overpayment race: two concurrent 6,000,000 payments against a 9,000,000 invoice -- exactly 1 succeeded, 1 correctly blocked, total paid stayed at 6,000,000; (D) the full return/refund lifecycle on a paid invoice -- confirmed the BUG-013 design works exactly as documented (a business_alert is raised naming the unsettled credit, AR is shown honestly negative rather than silently hidden, matching the projects own stated financial-integrity rule); (E) API authorization -- unauthenticated requests correctly 401, malformed ids correctly 400-validated, nonexistent document id correctly rejected (400 Not found -- logged as a P3 status-code-convention finding, not fixed: this exact badRequest(Not found) pattern is a pre-existing, widespread, non-security convention across dozens of routes repo-wide, out of scope for a sales-domain-focused fix). Zero regressions: full unit suite (1309/1309), lint, build, 9 governance audits, and all 22 committed regression suites all green."
    },
    {
      "id": "phase3b-sales-closure",
      "title": "Sales final verification: duplicate-audit-event fix, transaction rollback, customer-not-found 500",
      "priority": "P0",
      "status": "completed",
      "evidence": "Re-ran duplicate-invoice concurrency against the CURRENT build (not relying on a prior phase result): 5 concurrent identical invoice-create requests -> exactly 1 row, but found a REAL bug: audit_logs got 5 success rows for the 1 real creation, because logAction ran unconditionally after runOnce regardless of whether that specific call was the original write or a replayed duplicate. Same bug found WORSE in finance/journal create-and-post: a replayed duplicate also re-ran makerCheckerGate (creating a SECOND approval request per duplicate) and postEntryById (which correctly no-op'd but then made the route return a spurious 400 to what should have been a successful response). Fixed both with a didWrite flag set only inside the closure that actually executes (never set for a runOnce-short-circuited replay) gating logAction/post/approval side effects. Verified live: 5 concurrent invoice creates -> 1 row, 1 audit row (was 5); 5 concurrent create-and-post journal requests -> 1 entry, 1 audit row (was would-be 5), 0 duplicate approval requests. Forced-mid-transaction-rollback re-verified on a real payment-recording-shaped sequence (payment INSERT + document status UPDATE, forced failure before commit) -> zero orphan payment, zero status change, zero GL entries, zero audit rows -- ALL OR NOTHING confirmed with fresh evidence. Repo-wide inventory/COGS search re-confirmed fresh (not reused from Phase 3): zero linkage in either direction between sales and inventory code -- reservation/delivery/stock_movement/COGS all NOT_IMPLEMENTED as part of the sales flow. Full financial reconciliation with a fresh, odd-amount invoice (percentage discount+tax producing fractional Rial values) -- every layer matched exactly to the cent: subledger outstanding == GL net AR, revenue/tax subledger == GL revenue/tax postings, payment == GL bank debit == GL AR credit, and every entry balanced (debit==credit). A THIRD real defect found while testing the error catalog: a nonexistent customerId on invoice create fell through to a raw PG foreign-key-violation (23503, not special-cased by apiError) and surfaced as an unhandled HTTP 500 -- exactly the master prompt's own ERP-SALES-CUSTOMER-NOT-FOUND example. Fixed with a new catalog code + explicit existence check, verified live -> real HTTP 404 with the correct bilingual message. 1 new unit test (8 total in errors.test.ts). Zero regressions: unit 1310/1310, lint, build, 9 governance audits, all 22 regression suites all green."
    }
  ],
  "rule": "Every subsequent phase entry here will be updated to status=completed only after: real code change + tsc/lint/vitest/build/9 governance audits green + live-PostgreSQL verification + commit + push. No phase is marked completed on a claim alone."
}